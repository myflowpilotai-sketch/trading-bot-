import {ema,atr,rsi} from '../indicators/index.js';
export function analyze(frames){const h4=frames.H4||[],h1=frames.H1||[],m15=frames.M15||[],m5=frames.M5||[];if([h4,h1,m15,m5].some(x=>x.length<210))return null;const bias=(c)=>{const closes=c.map(x=>x.close);const e50=ema(closes,50),e200=ema(closes,200);return e50&&e200?(e50>e200?'BULLISH':'BEARISH'):'WAITING'};const b4=bias(h4),b1=bias(h1);const last=m15.at(-1),prev=m15.at(-2);const structure=last&&prev?(last.high>prev.high&&last.close>prev.high?'BULLISH BOS':last.low<prev.low&&last.close<prev.low?'BEARISH BOS':'RANGE'):'WAITING';const r=rsi(m5.map(x=>x.close));const a=atr(m5);let direction=null;if(b4==='BULLISH'&&b1==='BULLISH'&&structure==='BULLISH BOS'&&r!==null&&r<75)direction='LONG';if(b4==='BEARISH'&&b1==='BEARISH'&&structure==='BEARISH BOS'&&r!==null&&r>25)direction='SHORT';if(!direction)return {status:'NO_SIGNAL',reason:`H4 ${b4} · H1 ${b1} · M15 ${structure}`,biasH4:b4,biasH1:b1,structureM15:structure,confirmationM5:'WAITING'};const entry=m5.at(-1).close;const stop=direction==='LONG'?entry-(a||1.5)*1.5:entry+(a||1.5)*1.5;const target=direction==='LONG'?entry+(entry-stop)*2:entry-(stop-entry)*2;return {status:'LONG_CONFIRMED'===direction?'LONG_CONFIRMED':direction==='LONG'?'LONG_CONFIRMED':'SHORT_CONFIRMED',direction,entry,stop,target,rr:2,biasH4:b4,biasH1:b1,structureM15:structure,confirmationM5:'CONFIRMED',reason:`H4 ${b4} · H1 ${b1} · M15 ${structure} · RSI ${r.toFixed(1)}`,timestamp:new Date(last.time).toISOString()}}
